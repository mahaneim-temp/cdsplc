# 📱 CDS PLC 안드로이드 앱 - 설치 파일 안내

## 설치 파일 정보

**파일명: `cdsplc.apk`**

이 파일 하나만 휴대폰으로 전송하여 설치하면 됩니다!

---

## 🎯 간단한 설치 과정

### 받는 사람 (사용자)

1. **파일 받기**
   - 카카오톡, 텔레그램, 이메일 등으로 `cdsplc.apk` 파일 받기

2. **파일 열기**
   - 파일 관리자에서 `cdsplc.apk` 찾아서 탭하기

3. **설치 허용**
   - "알 수 없는 소스" 경고에서 **"허용"** 또는 **"설치 허용"** 탭하기

4. **설치 완료**
   - 설치 버튼 탭 → 자동 설치 → 완료!

5. **앱 실행**
   - 앱 목록에서 **"cdsplc"** 찾아서 실행

### 보내는 사람 (개발자)

1. **APK 빌드**
   - `android` 폴더에서 `build_apk.bat` 실행 (Windows)
   - 또는 Android Studio에서 빌드

2. **파일 확인**
   - 생성된 `cdsplc.apk` 파일 확인

3. **전송**
   - 메신저, 이메일, USB 등으로 전송

---

## 📋 빌드 방법 (개발자용)

### 방법 1: 자동 빌드 스크립트 (가장 쉬움)

**Windows:**
```bash
cd incinerator_controller/android
build_apk.bat
```

**Linux/Mac:**
```bash
cd incinerator_controller/android
chmod +x build_apk.sh
./build_apk.sh
```

### 방법 2: Android Studio 사용

1. Android Studio에서 `android` 폴더 열기
2. `Build > Build Bundle(s) / APK(s) > Build APK(s)`
3. 빌드 완료 후 `app/build/outputs/apk/debug/app-debug.apk` 파일을 `cdsplc.apk`로 이름 변경

### 방법 3: 명령줄 사용

```bash
cd incinerator_controller/android
gradlew.bat assembleDebug  # Windows
# 또는
./gradlew assembleDebug    # Linux/Mac

# 생성된 파일 이름 변경
copy app\build\outputs\apk\debug\app-debug.apk cdsplc.apk  # Windows
# 또는
cp app/build/outputs/apk/debug/app-debug.apk cdsplc.apk    # Linux/Mac
```

---

## ✅ 최종 결과

**파일 하나: `cdsplc.apk`**

- 메신저로 전송 가능
- 클릭 한 번으로 설치 가능
- 앱 목록에서 삭제 가능
- 복잡한 폴더 구조 불필요

---

## 📱 앱 정보

- **앱 이름**: cdsplc
- **파일 크기**: 약 5-10MB
- **필요 Android 버전**: 5.0 이상
- **인터넷 연결**: 불필요 (오프라인 작동)

---

## 🎉 완료!

이제 `cdsplc.apk` 파일 하나만 전송하면 누구나 쉽게 설치할 수 있습니다!

